# Runtime dependencies for Lambda zip only (Terraform / GitHub Actions).
# Use langchain-core — NOT the full "langchain" package (that bundle pulls huge vectorstore extras).
# Deployment uses S3 + Lambda (terraform) to avoid direct-upload size limits in CI.
# Full project: requirements.txt locally.
langgraph>=1.1.10
langchain-core>=0.3.0
langchain-aws>=0.2.0
langsmith>=0.1.0
langfuse>=2.0.0
launchdarkly-server-sdk>=9.0.0
boto3>=1.35.0
botocore>=1.35.0
pydantic>=2.7.0
faker>=26.0.0
PyJWT[crypto]>=2.8.0
fastapi>=0.115.0
mangum>=0.19.0
httpx>=0.27.0
mcp>=1.2.0,<2; python_version >= "3.10"
